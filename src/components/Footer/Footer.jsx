import React from 'react';
import style from "./Footer.module.css"
let Footer = (props) => {
    return (
        <div className={style.footer}>
            <span color={{color: "white"}}>Footer</span>
        </div>

    );

}
export default Footer;